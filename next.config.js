/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.ibb.co", "i.imgur.com", "culturavenezuela.com", "i0.wp.com", "upload.wikimedia.org"]
  }
}

module.exports = nextConfig
